generate_content:
  fields:
    config_entry:
      required: true
      selector:
        config_entry:
          integration: homestral
    prompt:
      required: true
      selector:
        text:
          multiline: true
      example: "Hello, how can I help you?"
